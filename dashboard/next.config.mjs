/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'i.pinimg.com',
            'media.giphy.com',
            'cdn.dribbble.com',
            'cdn.pixabay.com',
            'avatar.vercel.sh'
        ]
    }
};

export default nextConfig;
